import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.eoc.outingcoach",
  appName: "외출 도우미",
  // Capacitor bundles the statically-exported Next.js site from here.
  webDir: "out",
};

export default config;
